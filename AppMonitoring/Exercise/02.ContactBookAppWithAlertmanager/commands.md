The app from task 0.1 shoul be running before running the commands

1. `./alertmanager --config.file ./alertmanager-contactbook.yml `
2. `./prometheus --config.file=prometheus-contact-book-alerts.yml`
